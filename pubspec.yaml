name: altyazi_app
description: Videoya altyazı ekleme uygulaması
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  video_player: ^2.8.0
  file_picker: ^6.1.1
  share_plus: ^7.2.1
  path_provider: ^2.1.1
  cupertino_icons: ^1.0.6
  http: ^1.2.0

dev_dependencies:
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
