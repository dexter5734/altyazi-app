name: Build APK

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Repo'yu çek
        uses: actions/checkout@v4

      - name: Java kur (Flutter/Gradle için ŞART)
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: Flutter kur
        uses: subosito/flutter-action@v2
        with:
          flutter-version: '3.24.5'
          channel: 'stable'

      - name: Flutter sürümünü doğrula
        run: flutter --version

      - name: Native Android projesini oluştur (android/ klasörü eksikse üretir)
        # Sadece lib/ + pubspec.yaml kopyalandığı için android/ yok.
        # Bu adım CI'da android/build.gradle vb. dosyaları üretir.
        run: flutter create --platforms=android .

      - name: Bağımlılıkları indir
        run: flutter pub get

      - name: APK derle
        run: flutter build apk --release

      - name: APK'yı artifact olarak yükle
        uses: actions/upload-artifact@v4
        with:
          name: altyazi-app-apk
          path: build/app/outputs/flutter-apk/app-release.apk
          if-no-files-found: error